# Environment variables for API tokens
GITHUB_TOKEN=
GITLAB_TOKEN=
BITBUCKET_TOKEN=

# Database configuration
DATABASE_PATH=./data/stats.db

# Default time range for deployment counting (in days)
DEFAULT_DAYS=30

# Log level (error, warn, info, debug)
LOG_LEVEL=info
